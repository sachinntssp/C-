#include<bits/stdc++.h>
using namespace std;
void prnt(vector<int>v)
{
    cout<<"size "<<v.size()<<endl;
    for(int i=0;i<v.size();i++)
    {
        cout<<v[i]<<" ";
    }
}
int main()
{
    // pair <int,string> p;
    // // p=make_pair(2,"sachin");
    // p={3,"kumar"};
    // pair<int,int> ap[3];
    // ap[0] = {1,2};
    // ap[1] = {3,4};
    // ap[2] = {5,6};
    // swap(ap[0],ap[2]);
    // for(int i=0;i<3;i++)
    // {
    //     cout<<ap[i].first<<" "<<ap[i].second<<endl;
    // }
    // cout<<p.first<<" "<<p.second;

    vector<int> v;
    int n;
    // cin>>n;
    // for(int i=0;i<n;i++)
    // {
    //     int x;cin>>x;
    //     v.push_back(x);
    // }
    prnt(v);
}